# Claude settings
.claude/

# Virtual environment
datasci/

# Raw parquet data (too large — download via script)
data/ookla/raw/
data/ndt7/raw/

# Processed parquet (regenerate from notebooks)
data/ookla/processed/
data/ndt7/processed/

# Clean NDT7 parquet — copied in from E:/ndt7/data, never committed (~40 GB)
# Markdown/CSV under data/ndt7/ is still tracked.
data/ndt7/**/*.parquet

# Jupyter checkpoints
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# LaTeX build artifacts
docs/*.aux
docs/*.log
docs/*.toc
docs/*.out
docs/*.pdf

# DuckDB spill
.tmp/

# GADM geometry — ฟรีสำหรับงานวิชาการ แต่ห้ามแจกต่อ (ต่างจาก geoBoundaries ที่เป็น ODbL)
# ไฟล์นี้ copy มาจาก E:/ndt7/data/ph/ ใช้ในเครื่องได้ แต่ห้าม commit
data/geo/gadm41_*.geojson
